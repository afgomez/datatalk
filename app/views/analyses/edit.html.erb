<% form_for @analysis do |f| %>
  <%= render "shared/form_errors", :object => @analysis %>

  <%= render :partial => 'form_analysis_fields', :locals => {:f => f}%>

  <%= f.hidden_field :analysis_id -%>

  <%= f.submit "Responder" -%>

<% end %>
